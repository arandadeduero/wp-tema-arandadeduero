# NPM Configuration for Aranda de Duero Theme

# Use exact versions for reproducible builds
save-exact=true

# Prefer offline cache when available
offline=false

# Use strict SSL certificates
strict-ssl=true

# Registry
registry=https://registry.npmjs.org/

# Legacy peer deps handling
legacy-peer-deps=false
